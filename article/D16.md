<!-- ═══════════════════════════════════════════════════════════════
     Day 16 寫作導引(寫完後這整塊可刪；HTML 註解不會輸出到文章）
     性質：⑤實作 💻code ｜ 階段：實現 ｜ 分級：🔵 工程師線
     里程碑：這天結束，讀者手上有「一個會記憶 + 會用工具」的 agent。
     ⚠️ 這是 D14–17 密度最高的一天（校稿提醒：把「檔案落 Redis」控制在一個
        設計取捨小節，不要展開成第二主題，否則壓力爆表）。

     🚦 §0 硬門檻
       ① 主幹問題（一句話）：無狀態的 LLM 怎麼「記住」上下文？怎麼用 Azure Redis
          給它一個記憶，還能配合工具呼叫？
       ② 用真實案例回答：先演「失憶」的窘境 → 用 Redis 存 conversation history
          → memory + tool_call 結合的最小 agent（可運行 code + 畫面）。

     🔗 承先啟後（依 article-framework Tip 3）
       承前：D14/D15 讓 AI 會呼叫工具、還能把工具標準化成 MCP → 本篇給它記憶，
             讓工具用得上上下文。
       啟後：→ D17「分裂操作：任務拆解 / 工具分派」把單一 agent 升級成多工具分派。
             做成系列內鏈。

     ⚠️ 待補（作者提供前不要寫死）
       [ ] 「失憶」對照畫面：同一個 bot 有記憶 vs 沒記憶
       [ ] Azure Redis（Azure Cache for Redis）連線與存取的去識別化設定
       [ ] 「檔案落 Redis」的實際取捨數據（大小/成本），一句帶過即可

     🚫 紅線：公司不具名、不揭露上線日期、連線字串/金鑰一律遮蔽
     🧭 認知鐵則：講「AI 能記憶」是事實可具體；不誇大成「它真的懂你」
     📝 §3 正向敘事：「檔案落 Redis」寫成設計取捨（Redis 適合放什麼），不寫成踩坑
     ══════════════════════════════════════════════════════════════ -->

# 【AI 基礎應用 ③】AI 記憶的原理與實作：memory + tool_call 示範、Azure Redis 存工作階段

<!-- 前導 TL;DR（前 3–5 行）＝鉤子 + 結論先行。
     鉤子：這幾天做出來的、會呼叫工具的 AI，其實下一句就忘了上一句。
     結論：LLM 本身無狀態，「記憶」是你把歷史訊息存起來、每次帶回去。用 Azure Redis
           存工作階段，再接上工具呼叫，就有一個記得上下文、會動手的 agent。 -->

這幾天你讓 AI 會呼叫工具(D14)、還把工具標準化成 MCP(D15)。但只要多聊兩句就會發現：**它記不住**。你說「講個雞腿的笑話」它講了，接著你問「換工程師的呢」，它一臉茫然——因為它根本不記得你剛剛在聊笑話。

原因不是它笨，是 **LLM 天生無狀態（stateless）**：每一次呼叫，對模型來說都是全新的一次，上一輪說了什麼，它不會自己記得。

這篇把「記憶」這件事拆開：先講它的原理（其實很樸素），再用 **Azure Redis** 把工作階段存起來，最後接回昨天的工具呼叫——這天結束，你手上是一個**記得上下文、又會動手的 agent**。

## AI 為什麼轉頭就忘？

<!-- 節首答案：因為 LLM 是無狀態的——每次 API 呼叫都是獨立的，模型不保存任何
     跨呼叫的狀態；你不把歷史帶回去，它就沒有上下文。 -->

因為每一次 `chat.completions.create()`，對模型來說都是**獨立、互不相干**的一次請求。它不會在雲端某處替你保存「我們剛剛聊到哪」。你這次送什麼，它就只看得到什麼。

所以「AI 記得我」這件事，其實是個**錯覺**——是應用層每次都把之前的對話一起送回去，製造出「它記得」的效果。認清這點，記憶就從玄學變成工程問題。

## 記憶的本質是什麼？

<!-- 節首答案：記憶＝把歷史訊息存起來、每次呼叫時一起帶回去。不是模型「記得」，
     是你的程式「幫它帶」。 -->

把上面那句話翻成一行工程定義：**記憶 = 把歷史訊息存起來，下次呼叫時一起帶回去**。

```python
# 沒有記憶：每次都只送當前這句 → 模型看不到上下文
messages = [{"role": "user", "content": "換工程師的呢？"}]  # 「換……的」在指什麼？模型不知道

# 有記憶：把整段對話歷史帶回去 → 模型才有上下文
messages = [
    {"role": "user", "content": "講個雞腿的笑話"},
    {"role": "assistant", "content": "為什麼雞腿不去健身房？因為它怕練成雞胸。"},
    {"role": "user", "content": "換工程師的呢？"},          # 現在模型知道你要的是「工程師主題的笑話」
]
```

差別就這麼簡單。難的不是「要不要帶」，是**「歷史存在哪、怎麼快速存取、多人多對話怎麼分開」**——這就是 Redis 上場的地方。

## 怎麼用 Azure Redis 存工作階段？

<!-- 節首答案：用 Azure Cache for Redis，以「使用者/對話 ID」為 key，把該對話的
     訊息陣列存成一筆；每次呼叫前讀出來、呼叫後寫回去。
     視覺錨點：Redis 裡一筆 session 的樣子（key → messages）。待補。 -->

**Redis 是一個記憶體型的鍵值資料庫，讀寫極快**——正好適合「每次對話都要頻繁存取」的工作階段。做法是以「對話 ID」當 key，把該對話的訊息陣列存成一筆：

```python
import json, redis

# 連線 Azure Cache for Redis（連線字串走環境變數，勿寫死）
r = redis.Redis(host="<your>.redis.cache.windows.net", port=6380,
                password="<REDIS_KEY>", ssl=True)

def load_history(session_id: str) -> list:
    raw = r.get(f"chat:{session_id}")
    return json.loads(raw) if raw else []

def save_history(session_id: str, messages: list, ttl=3600):
    r.set(f"chat:{session_id}", json.dumps(messages), ex=ttl)  # ex：過期時間
```

以 `session_id` 為 key，**不同使用者、不同對話天然隔開**；加上 `ex`（TTL）讓舊對話自動過期，記憶就不會無限膨脹。

## 記憶接上工具呼叫後，agent 長什麼樣？

<!-- 節首答案：每次請求先從 Redis 讀歷史 → 帶著歷史 + 工具定義呼叫模型 → 若有
     tool_call 就執行並餵回 → 最後把更新後的歷史寫回 Redis。這是 D15 工具迴圈
     + D16 記憶的合體。視覺錨點：有記憶 vs 沒記憶的對照畫面。待補。 -->

把 D15 的工具迴圈和今天的記憶合起來，就是一個最小但完整的 agent：**讀歷史 → 帶歷史呼叫模型 → 有工具就執行並餵回 → 寫回歷史**。

```python
def chat(session_id: str, user_input: str) -> str:
    messages = load_history(session_id)              # ① 從 Redis 讀記憶
    messages.append({"role": "user", "content": user_input})

    resp = client.chat.completions.create(
        model="<your-deployment-name>", messages=messages, tools=tools,
    )
    msg = resp.choices[0].message

    if msg.tool_calls:                               # ② 模型自己決定用工具
        call = msg.tool_calls[0]
        result = get_joke(**json.loads(call.function.arguments))
        messages += [msg, {"role": "tool",
                           "tool_call_id": call.id, "content": str(result)}]
        msg = client.chat.completions.create(
            model="<your-deployment-name>", messages=messages, tools=tools,
        ).choices[0].message

    messages.append({"role": "assistant", "content": msg.content})
    save_history(session_id, messages)               # ③ 寫回記憶
    return msg.content
```

![有記憶 vs 沒記憶：同一個追問「換工程師的呢」，有記憶版正確接上上下文（截圖待補）](../docs/D16/memory_compare.png)
_（待補：作者的對照畫面——同一句追問，有/沒記憶的差別。）_

現在再問「換工程師的呢」，模型看得到前文、也還是會呼叫工具——**記憶讓工具用得上上下文**，這兩件事合起來才像個能用的助理。

## 該把什麼放進 Redis？一個設計取捨

<!-- 節首答案：Redis 適合放「小而頻繁存取」的工作階段狀態；大檔案（PDF、圖）
     不該塞進 Redis，改放物件儲存、Redis 只存 reference。
     📝 §3：寫成設計取捨（我在 A/B 之間選了 B，因為…），不是「我踩了坑」。 -->

實作到一半，你會遇到一個很自然的問題：使用者傳了一個檔案（PDF、圖片），這東西要不要也存進 Redis？

**答案是不要——這是一個該想清楚的設計取捨。** Redis 是記憶體資料庫，強項是「小而頻繁」的存取；把大檔案 blob 塞進去，會吃掉昂貴的記憶體、也拖慢它最該快的地方。比較好的分工是：

- **大檔案** → 放物件儲存（如 Azure Blob Storage），拿到一個 URL / reference。
- **Redis** → 只存這個 reference 和輕量的工作階段狀態。

同樣是「記住這個檔案」，選擇「Redis 存指標、Blob 存本體」而不是「全塞 Redis」，換來的是成本與速度——**這種取捨判斷，就是工程師在 AI 應用裡真正在做的事**。

## 帶走什麼？

<!-- 依 Tip 2：可遷移的帶走重點，每點對回主幹問題。 -->

- **LLM 無狀態**：「AI 記得你」是應用層每次把歷史帶回去製造的效果，不是模型自己記得。
- **記憶 = 存歷史 + 每次帶回**：難點不在概念，在「存哪、怎麼快取、多對話怎麼隔開」。
- **Redis 存工作階段**：以對話 ID 當 key 天然隔離、用 TTL 讓記憶自動過期，不會無限膨脹。
- **記憶 × 工具**：讀歷史 → 帶歷史呼叫 → 執行工具餵回 → 寫回歷史，是 agent 的最小完整迴圈。
- **放對地方的取捨**：小而頻繁的狀態放 Redis，大檔案放物件儲存只存 reference——判斷放哪，是你的活。

<!-- 收束 + 下一篇鉤子（geo-seo §6 / Tip 3）：
     一句話重述：給無狀態的 LLM 一個 Redis 記憶，它就記得上下文、工具也用得上。
     啟後鉤子（扣 D17）：但工具一多，一顆通用 prompt 就開始接不住了。 -->

現在你的 AI 記得上下文、也會用工具了。但當工具從一個變成五個、十個，你會撞到下一道牆：**一顆通用的 prompt，開始接不住這麼多工具**——它會選錯工具、把上下文塞爆、判斷變差。

怎麼讓 AI 自己把需求**拆解**、把每一步**分派**給專精的工具，同時讓 context 保持專注、成本還算得清楚？那是明天 D17 的核心——**分裂操作**。
