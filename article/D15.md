<!-- ═══════════════════════════════════════════════════════════════
     Day 15 寫作導引(寫完後這整塊可刪；HTML 註解不會輸出到文章）
     性質：⑤實作 💻code ｜ 階段：實現 ｜ 分級：🔵 工程師線
     里程碑：這天結束，讀者手上有「一個自己產的 MCP 工具，任何 MCP host 都能接」。

     🚦 §0 硬門檻
       ① 主幹問題（一句話）：D14 手刻的 tool 綁死在一支程式裡——怎麼把工具包成
          「任何 AI、任何應用都能接上來用」的標準介面？
       ② 用真實案例回答：用 Python 的 FastMCP 從 0 產一個 MCP server（stdio），
          暴露一個工具，再示範怎麼被 host 接上（可運行 code + 畫面）。

     🔗 承先啟後（依 article-framework Tip 3）
       承前：D14 讓 AI 會呼叫「一個手刻的工具」→ 本篇把工具升級成 MCP 標準介面。
       啟後：→ D16「讓 AI 記住上下文（memory + Azure Redis）」
             → D17「分裂操作：任務拆解 / 工具分派」會編排多個工具。做成系列內鏈。

     ⚑ 定位（生成草稿時套用）
       - MCP 是 Anthropic 提出的「開放標準」，跨生態（Claude、IDE、多種 agent 框架皆可接）。
       - 建 MCP server（FastMCP）是 provider-neutral 的；與 D14/D16/D17 用 Azure AI Foundry
         當教學載體不衝突——手法共通、換 host 也成立。
       - 埋英文關鍵字：MCP、Model Context Protocol、MCP server、tool、stdio transport。

     ⚠️ 待補
       [ ] MCP host 實際接上的畫面（Claude Desktop / IDE / 你用的 host 擇一，去識別化）
       [ ] Azure AI Foundry Agent Service 是否直接吃 MCP：生成時查證，不先寫死
       [ ] 若放 repo：完整 server code 獨立成 gist

     🚫 紅線：公司不具名、不揭露上線日期、連線字串/金鑰一律遮蔽
     🧭 認知鐵則：講「MCP 能做什麼」可具體；不替讀者指定「你一定要全面改用 MCP」
     ══════════════════════════════════════════════════════════════ -->

# 【AI 基礎應用 ②】MCP(Model Context Protocol):自己產一個 MCP 工具,把工具標準化

<!-- 前導 TL;DR（前 3–5 行）＝鉤子 + 結論先行。
     鉤子：昨天你手刻的那個工具，綁死在一支程式裡，換個 AI 就得重寫。
     結論：MCP 是工具的「標準插頭」——把工具寫成一個 MCP server，任何支援 MCP 的 host
           都能接上來用。今天用 Python FastMCP 從 0 產一個。 -->

昨天(D14)你讓 AI 呼叫了一個工具。但那個 `get_joke` 是**手刻、綁死在那支程式裡**的——換一個 AI、換一個專案、換一個同事來接,都得重寫一次。

有沒有一種**標準插頭**,讓工具寫一次、到處能接?有,它叫 **MCP(Model Context Protocol)**——一個由 Anthropic 提出、如今跨生態的開放標準。你把工具包成一個 **MCP server**,任何支援 MCP 的 host(Claude、各種 IDE、agent 框架)都能連上來用,不必為每個 AI 各寫一份。

今天用 Python 的 **FastMCP** 從 0 產一個 MCP 工具,跑起來給你看。

## 為什麼需要 MCP?手刻工具的問題在哪?

<!-- 節首答案：手刻工具綁死在單一程式與單一 AI；每換一個 host 就要重接一次。
     MCP 把工具標準化成一個獨立 server，一次寫、到處接。 -->

D14 的工具很好用,但它有兩個綁死:

1. **綁死在一支程式**:工具函式、schema、呼叫迴圈全寫在同一個檔案裡,別的專案要用就得複製貼上。
2. **綁死在一個 AI**:換一個模型、一個框架,tool 定義的格式、餵回結果的方式可能都不一樣。

MCP 用一個很好記的比喻解決它:**工具的 USB-C**。你把工具做成一個標準介面的 **server**,host 端(用工具的那一方)照同一套協定來接——**工具寫一次,任何 host 都能插上來用**。

一句話:**function calling 是「模型怎麼呼叫工具」的機制;MCP 是「工具怎麼被標準化封裝、跨 host 共用」的協定。**

## MCP 到底是什麼?一分鐘拆解

<!-- 節首答案：MCP 是一個 client-server 協定。MCP server 暴露 tools / resources / prompts；
     host(裡面的 MCP client)連上來，列出有哪些工具、呼叫它們。 -->

MCP 是一個 **client-server 協定**,三個角色記住就夠:

- **MCP server**:你寫的那一端,**暴露工具**(tools)、資料(resources)、提示範本(prompts)。
- **Host**:實際在跑 AI 的應用(Claude、IDE、你的 agent),裡面內建一個 **MCP client**。
- **傳輸(transport)**:client 和 server 怎麼通訊。本機最常用 **stdio**(標準輸入/輸出),遠端則走 HTTP。

流程是:host 啟動你的 server → client 問「你有哪些工具?」→ server 回一份清單 → AI 需要時,client 呼叫某個工具、拿回結果。**你只負責把工具寫好並暴露出去,連接與呼叫由協定處理。**

## 怎麼從 0 產一個 MCP 工具?

<!-- 節首答案：用 FastMCP，一個 @mcp.tool() 裝飾器就把普通 Python 函式變成 MCP 工具；
     mcp.run() 預設用 stdio transport 起 server。視覺錨點：terminal 起 server 的畫面。待補。 -->

用 Python 的 **FastMCP**,把 D14 那個 `get_joke` 變成標準工具,只要一個裝飾器:

```python
# server.py — 一個最小的 MCP server(stdio transport)
# 安裝：pip install mcp
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("joke-tools")           # 給這組工具一個名字

@mcp.tool()
def get_joke(topic: str) -> str:
    """從冷笑話大全裡查一則指定主題的冷笑話。

    Args:
        topic: 冷笑話主題，例如：雞腿、工程師
    """
    # 示意資料；實務上這裡會查你的資料庫 / API
    return {
        "雞腿": "為什麼雞腿不喜歡去健身房？因為它怕自己「練成雞胸」了。",
        "工程師": "工程師為什麼分不清萬聖節和聖誕節？因為 Oct 31 == Dec 25。",
    }.get(topic, "這個主題今天被冷氣凍住了，笑話端不出來。")

if __name__ == "__main__":
    mcp.run()                         # 預設就是 stdio transport
```

看懂三個重點:

- **`@mcp.tool()`** 把一個普通函式**升級成 MCP 工具**——函式簽章變成參數 schema、docstring 變成工具說明,host 就是靠這些決定何時呼叫、怎麼填參數(所以 docstring 要寫好)。
- **`FastMCP(...)`** 是這組工具的容器,可以掛很多個 `@mcp.tool()`。
- **`mcp.run()`** 起 server,預設走 **stdio**——host 用「啟動這個指令、透過標準輸入輸出溝通」的方式接它。

![用 FastMCP 起一個 MCP server,等待 host 連線(截圖待補)](../docs/D15/mcp_server_run.png)
_（待補：terminal 跑 `python server.py` 起 server 的畫面。）_

跟 D14 比,差別是:工具**從「埋在呼叫迴圈裡」變成一個獨立、可被任何 host 接的 server**。

## 怎麼把它接上 host 來用?

<!-- 節首答案：支援 MCP 的 host 用一段設定指到你的 server 指令；host 端的 MCP client
     負責啟動、列工具、呼叫。視覺錨點：host 接上後列出工具、呼叫成功的畫面。待補。 -->

支援 MCP 的 host(Claude 桌面版、多種 IDE、agent 框架)接 stdio server 的方式,通常就是一段設定,告訴它「用哪個指令把 server 起起來」:

```json
{
  "mcpServers": {
    "joke-tools": {
      "command": "python",
      "args": ["server.py"]
    }
  }
}
```

host 啟動後,它裡面的 MCP client 會自動列出 `joke-tools` 有哪些工具,AI 需要時就呼叫 `get_joke`——**你完全不用改 server,同一個 server 能被不同 host 共用**。這正是「工具寫一次、到處接」的兌現。

> 待補:放一張你實際用的 host(Claude Desktop / IDE / 你的 agent)接上 `joke-tools`、成功呼叫的畫面。Azure AI Foundry Agent Service 對 MCP 的支援程度,生成草稿時再查證後補一句,不先寫死。

## MCP 還是 function calling?什麼時候用哪個?

<!-- 節首答案：不是二選一。function calling 是模型呼叫工具的底層機制；MCP 是把工具
     標準化、跨 host 共用的封裝。單一專案自用 → function calling 就夠；工具要跨專案 / 跨
     AI / 給別人接 → 包成 MCP。🧭 留白：讓讀者自己判斷該不該全面改用。 -->

先說結論:**它們不是二選一,是不同層次。**

- **function calling(D14)**:模型「決定呼叫哪個工具、填什麼參數」的底層機制。
- **MCP(今天)**:把工具**標準化封裝成 server**,讓它跨專案、跨 AI、跨 host 共用。

一個簡單的判斷起點:

| 情境 | 比較適合 |
|---|---|
| 單一專案、工具只有這支程式在用 | function calling 直接寫就好 |
| 工具要給多個專案 / 多個 AI / 別的同事接 | 包成 MCP server |
| 想接現成的生態工具(檔案、Git、資料庫…) | 用別人寫好的 MCP server |

至於你的專案該不該全面改用 MCP——那要看你的工具會不會被重複接。**這篇只負責讓你會做一個;要不要用、用到多深,是你自己的判斷。**

## 帶走什麼?

<!-- 依 Tip 2：可遷移的帶走重點，每點對回主幹問題。 -->

- **MCP 是工具的標準插頭**:工具寫成一個 server、寫一次,任何支援 MCP 的 host 都能接。
- **`@mcp.tool()` 就是升級鍵**:普通 Python 函式 + 一個裝飾器 = 一個 MCP 工具;docstring 與型別註記就是它的 schema。
- **stdio 是本機最省事的 transport**:host 用「啟動指令 + 標準輸入輸出」接你的 server。
- **MCP ≠ function calling**:一個是機制、一個是標準化封裝;單專案自用選前者,要跨 host 共用選後者。
- **適用邊界**:工具會被重複接才值得包成 MCP;一次性、單專案的小工具,直接 function calling 更快。

<!-- 收束 + 下一篇鉤子（geo-seo §6 / Tip 3）：
     一句話重述：把工具包成 MCP server，寫一次、任何 host 都能接。
     啟後鉤子（扣 D16）：但你的 AI 接上工具後，還是「轉頭就忘」——下一句就忘了上一句。 -->

現在你的工具是標準件了,任何 host 都能接。但接上工具的 AI,還有一個沒解決的問題:**它每次對話都像第一次見面**——你上一句說的話,它下一句就忘了。

一個會用工具、卻沒有記憶的 AI,能做的事很有限。怎麼給無狀態的 LLM 一個記憶、還能配合工具呼叫——那是明天 D16 的事。
